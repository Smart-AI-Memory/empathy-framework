# Empathy Memory Dashboard API - Backend Dependencies
# Python 3.11+

# FastAPI framework
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# Pydantic for validation (aligned with main pyproject.toml)
pydantic>=2.0.0,<3.0.0
pydantic-settings>=2.0.0,<3.0.0

# CORS middleware
python-cors==1.0.0

# WebSocket support (included in uvicorn[standard])
websockets==12.0

# Authentication (JWT)
PyJWT[crypto]>=2.8.0  # Replaces python-jose - uses cryptography backend
passlib[bcrypt]==1.7.4

# Structured logging
structlog==24.1.0

# Redis client (for memory system)
redis==5.0.1

# Type hints (aligned with main pyproject.toml)
typing-extensions>=4.0.0,<5.0.0

# Development dependencies (optional)
# Uncomment for development:
# pytest==7.4.3
# pytest-asyncio==0.21.1
# pytest-cov==4.1.0
# httpx==0.26.0  # For testing FastAPI
# black==23.12.1
# ruff==0.1.9
# mypy==1.8.0
